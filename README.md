# To Do List App avec React.js

Pour initialiser le projet, suivez les étapes suivantes:

- Clonez le repository.
- Dans votre terminal, entrez la commande ```shell $ npm install``` .
- Puis ```shell $ npm start```.
